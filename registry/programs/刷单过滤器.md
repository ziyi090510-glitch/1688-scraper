# 刷单过滤器

## 判定规则
1. 用户名: t + *** + 数字 → 刷单
2. 评价内容 <8字 → 无效
3. 中文 <4字 → 无效
4. 同商品 ≥3次相同6-8字短句 → 刷单

## 使用方式
```python
from core.filter import FakeReviewFilter
f = FakeReviewFilter()
real, fake, dup = f.filter_all(reviews)
```
