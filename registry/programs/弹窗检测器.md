# 弹窗检测器

## 5维检测
1. 文本关键词（操作频繁、验证码、拖动滑块、人机验证）
2. DOM结构（nc_*滑块、captcha/verify元素）
3. URL重定向（verify/captcha/risk/login）
4. 页面标题（验证/安全/异常/登录）
5. baxia遮罩（computedStyle高z-index固定定位）

## 检测频率
- pg.goto() 前 + 后
- 评论滚动前 + 后
- 每个商品处理前

## 处理规则
弹窗 → 立即停止 → Bark通知 → 手动处理 → 绝不自动关闭
